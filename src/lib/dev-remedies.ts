// Dependency-free — bundled into client code.
// User-facing copy that names repo files or npm scripts must reference these,
// so a rename has one place to update.

export const CARD_SEED_FILE = 'src/db/cards.seed.ts';
export const SEED_CARDS_COMMAND = 'npm run seed:cards';
