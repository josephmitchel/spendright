node_modules
.next
out
build
next-env.d.ts
package-lock.json

# Generated by drizzle-kit. Whitespace is immaterial to correctness here — the
# snapshots are JSON.parse'd — but drizzle-kit rewrites these files in its own
# style on the next `db:generate`, so formatting them only produces churn that
# comes straight back.
drizzle
